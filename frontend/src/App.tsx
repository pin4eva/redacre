import React from "react";

const App = () => {
  return (
    <div className="container">
      <button disabled className="btn primary">
        button
      </button>
    </div>
  );
};

export default App;
